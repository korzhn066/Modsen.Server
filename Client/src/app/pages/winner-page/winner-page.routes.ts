import { Routes } from '@angular/router';
import { WinnerPageComponent } from './winner-page.component';

export const WINNER_PAGE_ROUTES: Routes = [
  { path: '', component: WinnerPageComponent },
];