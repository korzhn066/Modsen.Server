import { Routes } from '@angular/router';
import { RegistrationPageComponent } from './registration-page.component';

export const REGISTRATION_PAGE_ROUTES: Routes = [
  { path: '', component: RegistrationPageComponent },
];